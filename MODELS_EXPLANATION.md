# 🧠 הסבר מקיף על המודלים של SkillSight

## 📋 תוכן עניינים
1. [סקירה כללית](#סקירה-כללית)
2. [מהי גישת Pairwise?](#מהי-גישת-pairwise)
3. [DeBERTa v3 Base](#1-deberta-v3-base---המודל-המומלץ)
4. [RoBERTa Base](#2-roberta-base---מודל-חלופי)
5. [MODELV2 One-Pass](#3-modelv2-one-pass---מודל-ניסיוני)
6. [למה Pairwise עדיף על One-Pass?](#למה-pairwise-עדיף-על-one-pass)
7. [למה המשקלות כבדים?](#למה-קבצי-המשקלות-כבדים-כל-כך)
8. [השוואה מסכמת](#השוואה-מסכמת)

---

## 🎯 סקירה כללית

הפרויקט משתמש ב-**3 מודלים** לזיהוי מיומנויות מטקסטים של קורות חיים:

| מודל | גודל קובץ | Macro F1 | סוג גישה |
|------|-----------|----------|----------|
| **DeBERTa v3 Base** | ~704 MB | **97.07%** | Pairwise |
| **RoBERTa Base** | ~476 MB | **95.46%** | Pairwise |
| **MODELV2 One-Pass** | ~704 MB | **48.30%** | One-Pass |

---

## 🔄 מהי גישת Pairwise?

### הרעיון המרכזי
במקום לנסות לזהות את כל 136 המיומנויות בהרצה אחת, אנחנו שואלים את המודל שאלה פשוטה יותר:
> "האם הטקסט הזה מכיל את המיומנות הזו?"

### איך זה עובד - דוגמה מלאה

נניח שיש לנו את הטקסט:
```
"I have 5 years of experience with Python and Django. 
Built REST APIs and deployed using Docker."
```

**שלב 1: יוצרים זוגות (Pairs)**

המודל מקבל זוגות של `[מיומנות, טקסט]`:

```
Pair 1: ["Python", "I have 5 years of experience..."]   → ?
Pair 2: ["Django", "I have 5 years of experience..."]   → ?
Pair 3: ["Docker", "I have 5 years of experience..."]   → ?
Pair 4: ["AWS", "I have 5 years of experience..."]      → ?
...
Pair 136: ["GraphQL", "I have 5 years..."]              → ?
```

**שלב 2: לכל זוג - סיווג ל-3 קטגוריות**

```
┌────────────────────────────────────────────────────────────┐
│                    Output Classes                          │
├────────────────────────────────────────────────────────────┤
│  Label 0 (NONE)     = אין קשר בין הטקסט למיומנות          │
│  Label 1 (IMPLICIT) = המיומנות נרמזת/משתמעת מההקשר        │
│  Label 2 (EXPLICIT) = המיומנות מוזכרת במפורש בטקסט        │
└────────────────────────────────────────────────────────────┘
```

**שלב 3: תוצאות הדוגמה**
```
["Python", text]  → 2 (EXPLICIT) ✅ כי "Python" מוזכר במפורש
["Django", text]  → 2 (EXPLICIT) ✅ כי "Django" מוזכר במפורש
["Docker", text]  → 2 (EXPLICIT) ✅ כי "Docker" מוזכר במפורש
["REST API", text]→ 2 (EXPLICIT) ✅ כי "REST APIs" מוזכר
["AWS", text]     → 0 (NONE)     ❌ לא מוזכר בטקסט
["DevOps", text]  → 1 (IMPLICIT) 🔶 משתמע מ-Docker deployment
```

### הקוד בפועל - כיצד יישמנו

**קוד הזנת זוג למודל** (מתוך `analyze_resume.py`):
```python
def predict_pairwise(model_dir: Path, text: str, skills: list):
    tokenizer = AutoTokenizer.from_pretrained(str(model_dir))
    model = AutoModelForSequenceClassification.from_pretrained(str(model_dir))
    
    id_to_score = {0: 0.0, 1: 0.5, 2: 1.0}  # NONE=0, IMPLICIT=0.5, EXPLICIT=1.0
    results = {}
    
    for skill in skills:  # עוברים על כל 136 המיומנויות
        # הטוקנייזר מקבל זוג: (skill, text)
        enc = tokenizer(skill, text, truncation=True, max_length=384, 
                       padding=True, return_tensors="pt")
        
        out = model(**enc)  # forward pass
        pred_id = torch.argmax(out.logits, dim=-1).item()  # 0, 1, או 2
        results[skill] = id_to_score[pred_id]
    
    return results
```

### מבנה הקלט הפנימי למודל

כשאנחנו קוראים `tokenizer(skill, text)`, נוצר הקלט הבא:

```
┌─────────────────────────────────────────────────────────────────────┐
│  Input Format for Transformer                                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  [CLS] Python [SEP] I have 5 years of experience with ... [SEP]    │
│    ↑      ↑     ↑                      ↑                     ↑     │
│  Token  Skill  Sep                   Text                  End     │
│  מיוחד  שם     הפרדה                 תוכן הקורות חיים      סיום   │
│                                                                     │
│  Token IDs:  [101, 18750, 102, 1045, 1138, 1019, 2086, ...]        │
│  Attention:  [1,   1,     1,   1,    1,    1,    1,    ...]        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 1. DeBERTa v3 Base - המודל המומלץ ✅

### 🔬 מה זה DeBERTa?
**DeBERTa** (Decoding-enhanced BERT with disentangled attention) הוא מודל שפה מתקדם שפותח על ידי **Microsoft** ב-2020.

### 🧬 איך DeBERTa עובד - הסבר האלגוריתם

#### אלגוריתם 1: Disentangled Attention (תשומת לב מופרדת)

הרעיון המהפכני של DeBERTa: **להפריד את התוכן מהמיקום**.

ב-BERT רגיל, כל מילה מיוצגת כ:
```
embedding = content + position
```

ב-DeBERTa, מפרידים אותם:
```
content_embedding = מה המילה אומרת
position_embedding = איפה המילה נמצאת
```

**למה זה עוזר?** דוגמה:

```
משפט 1: "Python experience required"
משפט 2: "Required Python experience"
```

שני המשפטים אומרים אותו דבר, אבל המיקומים שונים!
DeBERTa מבין שהתוכן זהה למרות המיקום השונה.

#### איך חישוב ה-Attention עובד ב-DeBERTa

```
┌─────────────────────────────────────────────────────────────────┐
│               DeBERTa Disentangled Attention                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Standard BERT Attention:                                       │
│  ────────────────────────                                       │
│  A = softmax(Q × K^T / √d)                                     │
│  Q, K מכילים תוכן+מיקום מעורבבים                                │
│                                                                 │
│  DeBERTa Attention (4 רכיבים נפרדים):                          │
│  ────────────────────────────────────                           │
│  A = softmax((Q_c × K_c^T)   +    # תוכן-לתוכן                 │
│              (Q_c × K_p^T)   +    # תוכן-למיקום                │
│              (Q_p × K_c^T)   +    # מיקום-לתוכן                │
│              (Q_p × K_p^T))      # מיקום-למיקום                │
│                                                                 │
│  Q_c = Query של תוכן     K_c = Key של תוכן                     │
│  Q_p = Query של מיקום    K_p = Key של מיקום                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### אלגוריתם 2: Enhanced Mask Decoder (EMD)

בזמן אימון, DeBERTa מוסיף מידע על המיקום המוחלט רק בשכבה האחרונה:

```python
# Pseudocode של EMD
def enhanced_mask_decoder(hidden_states, absolute_positions):
    # 11 שכבות ראשונות - רק relative position
    for layer in layers[:-1]:
        hidden = layer(hidden, relative_position_only=True)
    
    # שכבה אחרונה - מוסיפים absolute position
    final_layer = layers[-1]
    output = final_layer(hidden, absolute_position=absolute_positions)
    
    return output
```

**למה זה עוזר?** המודל לומד דפוסים כלליים יותר בשכבות הראשונות.

### 📊 מפרט טכני

| פרמטר | ערך | הסבר |
|-------|-----|------|
| מספר שכבות (Layers) | 12 | 12 Transformer blocks |
| Attention Heads | 12 | מסתכלים על הטקסט מ-12 זוויות |
| Hidden Size | 768 | גודל הייצוג הפנימי של כל טוקן |
| Vocabulary Size | 128,100 | כמות המילים/חלקי מילים המוכרים |
| Max Sequence Length | 512 | אורך מקסימלי של קלט |
| מספר פרמטרים | ~184 מיליון | כל המשקלות הניתנות ללמידה |
| גודל קובץ | **~704 MB** | 184M × 4 bytes ≈ 736MB |

### 🎓 איך אימנו את DeBERTa

**קובץ האימון: `src/training/model_creation.py`**

```python
# שלב 1: טעינת המודל הבסיסי מ-HuggingFace
model = AutoModelForSequenceClassification.from_pretrained(
    "microsoft/deberta-v3-base", 
    num_labels=3  # NONE, IMPLICIT, EXPLICIT
)

# שלב 2: יצירת זוגות לאימון
def build_pairs(examples, global_skills, neg_ratio=4.0):
    rows = []
    for ex in examples:
        text = ex["job_description"]
        skills_dict = ex["skills"]  # {"Python": 1.0, "AWS": 0.5, ...}
        
        # זוגות חיוביים (מיומנויות שקיימות)
        for skill, score in skills_dict.items():
            label = score_to_label(score)  # 0, 1, או 2
            if label > 0:
                rows.append({
                    "skill": skill, 
                    "job_description": text, 
                    "label": label
                })
        
        # זוגות שליליים (מיומנויות שלא קיימות)
        negatives = sample_negatives(global_skills, skills_dict, ratio=4)
        for skill in negatives:
            rows.append({
                "skill": skill, 
                "job_description": text, 
                "label": 0
            })
    
    return rows
```

**שלב 3: טוקניזציה**
```python
def tokenize_pairs(tokenizer, rows, max_length=384):
    skills = [r["skill"] for r in rows]      # ["Python", "AWS", ...]
    texts = [r["job_description"] for r in rows]
    labels = [r["label"] for r in rows]
    
    # יוצר את הקלט בפורמט [CLS] skill [SEP] text [SEP]
    encodings = tokenizer(skills, texts, 
                          truncation=True, 
                          max_length=max_length)
    
    return PairDataset(encodings, labels)
```

**שלב 4: אימון עם HuggingFace Trainer**
```python
training_args = TrainingArguments(
    output_dir="models/deberta_v3_base",
    num_train_epochs=3,
    learning_rate=2e-5,
    per_device_train_batch_size=8,
    eval_strategy="epoch",
    save_strategy="epoch",
    load_best_model_at_end=True,
    metric_for_best_model="macro_f1",
    fp16=True,  # חצי דיוק לחיסכון בזיכרון
)

trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=train_ds,
    eval_dataset=val_ds,
    compute_metrics=compute_metrics,  # מחשב F1, Precision, Recall
)

trainer.train()  # מתחיל אימון
trainer.save_model("models/deberta_v3_base")  # שומר את המשקלות
```

### 🎯 תוצאות על הנתונים שלנו

```
              precision    recall  f1-score   support
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
No Match (0)    0.9902    0.9802    0.9852     9,556
Implicit (1)    0.8997    0.9477    0.9231     1,779
Explicit (2)    0.9951    0.9967    0.9959       610
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Accuracy                            0.9762    11,945
Macro F1                            0.9681
Best Validation F1                  0.9707
```

### 💡 למה DeBERTa הכי טוב?

1. **Disentangled Attention**: מבין הקשרים גם כשהמיקום משתנה
2. **Vocabulary גדול (128K)**: מכיר יותר מונחים טכניים
3. **Enhanced Mask Decoder**: למידה יציבה יותר
4. **Relative Position Encoding**: מבין יחסים בין מילים רחוקות

---

## 2. RoBERTa Base - מודל חלופי

### 🔬 מה זה RoBERTa?
**RoBERTa** (Robustly Optimized BERT) הוא מודל שפותח על ידי **Facebook AI** ב-2019. הוא גרסה משופרת של BERT עם אימון אופטימלי יותר.

### 🧬 איך RoBERTa עובד - הסבר האלגוריתם

RoBERTa הוא בעצם BERT עם שינויים באופן האימון:

#### אלגוריתם 1: Dynamic Masking (מיסוך דינמי)

**ב-BERT המקורי:**
```python
# הנתונים נוצרו פעם אחת עם מסכות קבועות
masked_data = create_masked_data(corpus)  # מריצים פעם אחת
for epoch in range(10):
    train(model, masked_data)  # אותן מסכות בכל epoch
```

**ב-RoBERTa:**
```python
# מסכות משתנות בכל epoch!
for epoch in range(10):
    masked_data = create_masked_data(corpus)  # יוצרים מסכות חדשות
    train(model, masked_data)  # מסכות שונות בכל פעם
```

**למה זה עוזר?** המודל רואה את אותן מילים במצבים שונים → למידה מגוונת יותר.

#### אלגוריתם 2: ללא Next Sentence Prediction (NSP)

**ב-BERT המקורי:**
```python
# משימה 1: חיזוי מילים מוסתרות
loss1 = masked_language_model(text)

# משימה 2: האם משפט B בא אחרי משפט A? (לא יעיל!)
loss2 = next_sentence_prediction(sentence_A, sentence_B)

total_loss = loss1 + loss2
```

**ב-RoBERTa:**
```python
# רק משימה 1! יותר יעיל
loss = masked_language_model(text)
```

המחקר הראה ש-NSP לא עוזר ואפילו מזיק לביצועים.

### 📊 מפרט טכני

| פרמטר | ערך | השוואה ל-DeBERTa |
|-------|-----|------------------|
| מספר שכבות (Layers) | 12 | זהה |
| Attention Heads | 12 | זהה |
| Hidden Size | 768 | זהה |
| Vocabulary Size | 50,265 | **פחות מ-DeBERTa (128K)** |
| Max Sequence Length | 514 | דומה |
| מספר פרמטרים | ~125 מיליון | **פחות מ-DeBERTa (184M)** |
| גודל קובץ | **~476 MB** | **228MB פחות!** |

### 🎓 איך אימנו את RoBERTa

אותו תהליך בדיוק כמו DeBERTa, רק עם מודל בסיס שונה:

```python
# ההבדל היחיד - שם המודל
model = AutoModelForSequenceClassification.from_pretrained(
    "roberta-base",  # במקום "microsoft/deberta-v3-base"
    num_labels=3
)
```

**פקודת האימון:**
```powershell
python src/training/model_creation.py --train \
    --model roberta-base \
    --out models/roberta_base \
    --epochs 3 \
    --lr 2e-5 \
    --batch 8
```

### 🎯 תוצאות

- **Best Validation Macro F1**: 95.46%
- **Best Checkpoint**: checkpoint-20826

### 💡 יתרונות וחסרונות

| יתרון | חסרון |
|-------|-------|
| קובץ קטן יותר (476MB) | F1 נמוך ב-1.6% מ-DeBERTa |
| מהיר יותר באימון | Vocabulary קטן יותר (50K vs 128K) |
| פחות זיכרון GPU | לא Disentangled Attention |
| אימון יציב | Position Encoding פחות מתוחכם |

---

## 3. MODELV2 One-Pass - מודל ניסיוני ❌

### 🔬 מה זה One-Pass?
גישת **One-Pass** מנסה לזהות את כל המיומנויות בהרצה אחת, במקום לבדוק כל מיומנות בנפרד.

### 🧬 איך One-Pass עובד - הסבר האלגוריתם

#### ארכיטקטורת הרשת

```python
# מתוך src/MODELV2/modelv2.py

class OnePassSkillClassifier(nn.Module):
    def __init__(self, base_name: str, num_skills: int, dropout: float = 0.1):
        super().__init__()
        # שכבה 1: מודל בסיס (DeBERTa)
        self.base = AutoModel.from_pretrained(base_name)
        hidden = self.base.config.hidden_size  # 768
        
        self.num_skills = num_skills  # 136 מיומנויות
        self.dropout = nn.Dropout(dropout)
        
        # שכבה 2: Classification Head
        # מוציא 136 × 3 = 408 ערכים!
        self.head = nn.Linear(hidden, num_skills * 3)
        
        self.loss_fn = nn.CrossEntropyLoss()
```

#### Forward Pass - מה קורה בהרצה

```python
def forward(self, input_ids, attention_mask, labels=None):
    # שלב 1: מעבירים את הטקסט דרך Transformer
    out = self.base(input_ids=input_ids, attention_mask=attention_mask)
    
    # שלב 2: לוקחים רק את ה-CLS token
    last_hidden = out.last_hidden_state  # צורה: [Batch, Tokens, 768]
    cls = last_hidden[:, 0, :]           # צורה: [Batch, 768]
    
    # שלב 3: Dropout למניעת overfitting
    x = self.dropout(cls)
    
    # שלב 4: Classification Head
    logits = self.head(x)  # צורה: [Batch, 136 × 3 = 408]
    
    # שלב 5: Reshape לפורמט [Batch, 136 skills, 3 classes]
    logits = logits.view(-1, self.num_skills, 3)
    
    # שלב 6: חישוב Loss
    if labels is not None:
        # labels צורה: [Batch, 136] - ערכים 0,1,2 לכל skill
        B, S = labels.shape
        flat_logits = logits.reshape(B * S, 3)  # [Batch×136, 3]
        flat_labels = labels.reshape(B * S)     # [Batch×136]
        loss = self.loss_fn(flat_logits, flat_labels)
    
    return {"loss": loss, "logits": logits}
```

### 📐 השוואה ויזואלית: Pairwise vs One-Pass

```
┌─────────────────────────────────────────────────────────────────────┐
│                   Pairwise (DeBERTa/RoBERTa)                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Input:                                                             │
│  ┌──────────────────────────────────────────────────┐              │
│  │ [CLS] Python [SEP] I have experience with... [SEP]│              │
│  └──────────────────────────────────────────────────┘              │
│                          ↓                                          │
│  ┌──────────────────────────────────────────────────┐              │
│  │         Transformer (12 layers)                   │              │
│  └──────────────────────────────────────────────────┘              │
│                          ↓                                          │
│  Output: [0.02, 0.08, 0.90] → Class 2 (EXPLICIT)                   │
│                                                                     │
│  מריצים 136 פעמים (פעם לכל מיומנות!)                                │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                      One-Pass (MODELV2)                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Input:                                                             │
│  ┌──────────────────────────────────────────────────┐              │
│  │ [CLS] I have experience with Python... [SEP]      │              │
│  └──────────────────────────────────────────────────┘              │
│                          ↓                                          │
│  ┌──────────────────────────────────────────────────┐              │
│  │         Transformer (12 layers)                   │              │
│  └──────────────────────────────────────────────────┘              │
│                          ↓                                          │
│  ┌──────────────────────────────────────────────────┐              │
│  │      Linear Layer: 768 → 408 (136 × 3)           │              │
│  └──────────────────────────────────────────────────┘              │
│                          ↓                                          │
│  Output: [[.9,.05,.05], [.1,.8,.1], ...] (136 predictions!)        │
│           Python=0     Django=1    ...                              │
│                                                                     │
│  מריצים פעם אחת בלבד!                                               │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 📊 מפרט טכני

| פרמטר | ערך |
|-------|-----|
| בסיס | microsoft/deberta-v3-base |
| Epochs | 3 |
| Learning Rate | 2e-05 |
| Batch Size | 8 |
| Max Length | 384 |
| Output Size | 136 × 3 = 408 |

### 🎯 תוצאות על הנתונים שלנו

```
              precision    recall  f1-score   support
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
No Match (0)    0.9703    0.9996    0.9848    68,978
Implicit (1)    0.8485    0.1088    0.1928     1,802
Explicit (2)    0.8962    0.1532    0.2617       620
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Macro F1                            0.4798
Accuracy                            0.9698
```

### ⚠️ למה הביצועים גרועים?

#### בעיה 1: חוסר איזון קיצוני בנתונים

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Class Distribution                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  No Match (0): ████████████████████████████████████████ 96.6%      │
│  Implicit (1): ██ 2.5%                                              │
│  Explicit (2): █ 0.9%                                               │
│                                                                     │
│  המודל לומד פשוט להגיד "0" לכל דבר!                                 │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**בנתוני האימון:**
- 68,978 דוגמאות של "No Match" (96.6%)
- 1,802 דוגמאות של "Implicit" (2.5%)
- 620 דוגמאות של "Explicit" (0.9%)

המודל לומד לחזות "0" תמיד כי זה נכון ב-97% מהמקרים!

#### בעיה 2: Recall נמוך מאוד

```
Implicit: Recall = 10.88% → מזהה רק 1 מכל 10!
Explicit: Recall = 15.32% → מזהה רק 1.5 מכל 10!
```

המודל מפספס את רוב המיומנויות האמיתיות.

#### בעיה 3: Multi-Label קשה מדי

ב-Pairwise: המודל לומד שאלה אחת פשוטה
```
"האם Python קיים בטקסט?" → כן/לא/משתמע
```

ב-One-Pass: המודל לומד 136 שאלות בו-זמנית
```
"האם Python קיים?" + "האם AWS קיים?" + "האם Docker קיים?" + ...
```

זה דורש הרבה יותר נתונים והרבה יותר אימון.

---

## ⚖️ למה Pairwise עדיף על One-Pass?

### 1. פישוט הבעיה

| גישה | מה המודל לומד | קושי |
|------|--------------|------|
| Pairwise | "האם skill X קיים בטקסט?" | **קל** - שאלה בינארית |
| One-Pass | "אילו מ-136 skills קיימים?" | **קשה** - Multi-label |

### 2. איזון נתונים

**Pairwise:**
```python
# שולטים ביחס בין דוגמאות חיוביות לשליליות
neg_ratio = 4.0  # 4 שליליות לכל חיובית
positives = [("Python", text, 2), ("Django", text, 1)]
negatives = sample(4 * len(positives), from_all_skills)
# יחס: 1:4 - הרבה יותר מאוזן!
```

**One-Pass:**
```python
# אין שליטה - היחס נקבע מהנתונים
# לכל טקסט יש ~3-5 מיומנויות מתוך 136
# יחס אמיתי: 1:40+ - לא מאוזן בכלל!
```

### 3. סיגנל למידה

**Pairwise:**
```
כל forward pass → loss על שאלה אחת ספציפית
הגרדיאנט: "תלמד לזהות Python טוב יותר"
```

**One-Pass:**
```
כל forward pass → loss ממוצע על 136 שאלות
הגרדיאנט: "תלמד לזהות הכל בינוני" 😕
```

### 4. השוואה מספרית

| מדד | Pairwise (DeBERTa) | One-Pass |
|-----|-------------------|----------|
| **Macro F1** | 97.07% | 48.30% |
| **Recall (Implicit)** | 94.77% | 10.88% |
| **Recall (Explicit)** | 99.67% | 15.32% |
| מהירות הרצה | איטי (136 passes) | מהיר (1 pass) |
| איכות | ✅ מעולה | ❌ גרועה |

### 5. מתי One-Pass יכול לעבוד?

One-Pass יכול לעבוד אם:
- יש הרבה יותר נתונים (פי 10-100)
- משתמשים ב-Class Weights או Focal Loss
- עושים Data Augmentation
- מאמנים יותר epochs

**אבל לנתונים הנוכחיים שלנו - Pairwise פשוט עדיף!**

---

## 💾 למה קבצי המשקלות כבדים כל כך?

### 🧮 מה יש בתוך קובץ המשקלות?

קובץ `model.safetensors` מכיל את כל הפרמטרים הנלמדים של הרשת:

```
┌─────────────────────────────────────────────────────────────────────┐
│                    מבנה model.safetensors                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. EMBEDDINGS LAYER (~98-392 MB)                                  │
│  ═══════════════════════════════                                    │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Token Embeddings:                                           │   │
│  │  • DeBERTa: 128,100 tokens × 768 dims = 98.4 million params │   │
│  │  • RoBERTa: 50,265 tokens × 768 dims = 38.6 million params  │   │
│  │                                                              │   │
│  │  Position Embeddings:                                        │   │
│  │  • DeBERTa: Relative (מורכב יותר, ~100MB נוספים)            │   │
│  │  • RoBERTa: Absolute (512 × 768 = 393K params)              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  2. 12 TRANSFORMER LAYERS (~350-400 MB)                            │
│  ══════════════════════════════════════                             │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  כל שכבה מכילה:                                              │   │
│  │                                                              │   │
│  │  Self-Attention:                                             │   │
│  │  • Q (Query):  768 × 768 = 589,824 params                   │   │
│  │  • K (Key):    768 × 768 = 589,824 params                   │   │
│  │  • V (Value):  768 × 768 = 589,824 params                   │   │
│  │  • Output:     768 × 768 = 589,824 params                   │   │
│  │  סה"כ Attention: 2.36M params × 12 layers = 28.3M           │   │
│  │                                                              │   │
│  │  Feed-Forward Network:                                       │   │
│  │  • Linear1: 768 × 3072 = 2.36M params                       │   │
│  │  • Linear2: 3072 × 768 = 2.36M params                       │   │
│  │  סה"כ FFN: 4.72M params × 12 layers = 56.6M                 │   │
│  │                                                              │   │
│  │  LayerNorm: ~3K params × 4 × 12 = 144K                      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  3. CLASSIFICATION HEAD (~6 KB)                                    │
│  ══════════════════════════════                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Linear: 768 → 3 = 2,307 params                             │   │
│  │  (זניח לעומת השאר!)                                          │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 🔢 חישוב מתמטי מפורט

#### DeBERTa v3 Base:

```python
# Token Embeddings
token_embeddings = vocab_size × hidden_size
                 = 128,100 × 768
                 = 98,380,800 params

# Relative Position Embeddings (ייחודי ל-DeBERTa)
# מורכב יותר - כולל position buckets ומטריצות נוספות
relative_pos = ~25,000,000 params  # הערכה

# 12 Transformer Layers
attention_per_layer = 4 × (768 × 768) = 2,359,296
ffn_per_layer = 2 × (768 × 3072) = 4,718,592
layernorm_per_layer = 4 × 768 × 2 = 6,144
total_per_layer = 7,084,032
all_layers = 12 × 7,084,032 = 85,008,384 params

# Classification Head
classifier = 768 × 3 + 3 = 2,307 params

# סה"כ
total = 98.4M + 25M + 85M + 0.002M ≈ 184M params

# בבתים (float32 = 4 bytes)
file_size = 184,000,000 × 4 = 736 MB
# אחרי דחיסה: ~704 MB
```

#### RoBERTa Base:

```python
# Token Embeddings
token_embeddings = 50,265 × 768 = 38,603,520 params

# Absolute Position Embeddings
position_embeddings = 514 × 768 = 394,752 params

# 12 Transformer Layers (אותו מבנה)
all_layers = 85,008,384 params

# Classification Head
classifier = 2,307 params

# סה"כ
total = 38.6M + 0.4M + 85M + 0.002M ≈ 125M params

# בבתים
file_size = 125,000,000 × 4 = 500 MB
# אחרי דחיסה: ~476 MB
```

### 🤔 למה DeBERTa גדול יותר ב-228MB?

| רכיב | DeBERTa | RoBERTa | הפרש |
|------|---------|---------|------|
| **Vocabulary** | 128,100 | 50,265 | +77,835 tokens |
| Token Embeddings | 98.4 MB | 38.6 MB | **+59.8 MB** |
| Position Type | Relative | Absolute | - |
| Position Size | ~25M params | 0.4M params | **+~100 MB** |
| Disentangled Matrices | כן | לא | **+~50 MB** |
| **סה"כ** | 704 MB | 476 MB | **+228 MB** |

### 📦 למה float32 ולא משהו קטן יותר?

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Data Types & Precision                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  float32 (4 bytes) - מה שאנחנו משתמשים:                            │
│  • דיוק: 7 ספרות עשרוניות                                          │
│  • טווח: ±3.4 × 10³⁸                                               │
│  • יתרון: יציבות מספרית מעולה                                      │
│  • חיסרון: גודל קובץ                                               │
│                                                                     │
│  float16 (2 bytes) - אפשרי לחיסכון:                                │
│  • דיוק: 3 ספרות עשרוניות                                          │
│  • יתרון: חצי מהגודל! (350MB במקום 700MB)                          │
│  • חיסרון: עלול לגרום ל-overflow/underflow                         │
│                                                                     │
│  int8 (1 byte) - Quantization:                                     │
│  • יתרון: רבע מהגודל! (175MB)                                      │
│  • חיסרון: ירידה באיכות של 1-3%                                    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 💡 האם אפשר להקטין?

**כן! אפשר לעשות Quantization:**

```python
# דוגמה לקוונטיזציה (לא מיושם בפרויקט)
from transformers import AutoModelForSequenceClassification
import torch

model = AutoModelForSequenceClassification.from_pretrained("models/deberta_v3_base")

# Dynamic Quantization
quantized_model = torch.quantization.quantize_dynamic(
    model, 
    {torch.nn.Linear},  # רק שכבות Linear
    dtype=torch.qint8
)

# גודל חדש: ~175MB במקום 704MB!
# אבל: ירידה של ~1-2% ב-F1
```

**למה לא עשינו את זה?**
- 704MB עדיין סביר
- לא רצינו לסכן את הדיוק של 97%
- Quantization דורש בדיקות נוספות

---

## 📈 השוואה מסכמת

### 🏆 טבלת ביצועים מלאה

| מדד | DeBERTa v3 | RoBERTa | MODELV2 One-Pass |
|-----|------------|---------|------------------|
| **Macro F1** | **97.07%** | 95.46% | 48.30% |
| **Accuracy** | 97.62% | ~95% | 96.98% |
| **Precision (No Match)** | 99.02% | ~98% | 97.03% |
| **Recall (No Match)** | 98.02% | ~97% | 99.96% |
| **Precision (Implicit)** | 89.97% | ~88% | 84.85% |
| **Recall (Implicit)** | **94.77%** | ~92% | 10.88% ❌ |
| **Precision (Explicit)** | 99.51% | ~98% | 89.62% |
| **Recall (Explicit)** | **99.67%** | ~98% | 15.32% ❌ |

### 📊 מה המספרים אומרים?

```
┌─────────────────────────────────────────────────────────────────────┐
│                    הסבר על המדדים                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Precision = כמה מהחיזויים שלנו נכונים?                            │
│  ════════════════════════════════════                               │
│  מתוך כל הפעמים שהמודל אמר "יש Python" - כמה פעמים צדק?           │
│                                                                     │
│  DeBERTa: 99% → כמעט תמיד צודק כשהוא מזהה מיומנות!                 │
│  MODELV2: 85% → 15% False Positives                                │
│                                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│  Recall = כמה מהמיומנויות האמיתיות מצאנו?                          │
│  ═══════════════════════════════════════                            │
│  מתוך כל הפעמים שבאמת יש Python - כמה זיהינו?                      │
│                                                                     │
│  DeBERTa: 95-99% → מוצא כמעט הכל!                                  │
│  MODELV2: 10-15% → מפספס 85-90% מהמיומנויות!                       │
│                                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│  Macro F1 = ממוצע הרמוני של Precision ו-Recall                     │
│  ═══════════════════════════════════════════                        │
│  נותן משקל שווה לכל class, גם אם הם לא מאוזנים                     │
│                                                                     │
│  DeBERTa: 97% → ביצועים מעולים בכל הקטגוריות                       │
│  MODELV2: 48% → Recall נמוך הורס את הציון                          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### ⚖️ מתי להשתמש בכל מודל?

```
┌─────────────────────────────────────────────────────────────────────┐
│                   המלצות שימוש                                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ✅ DeBERTa v3 Base (מומלץ!):                                      │
│     • כשצריך דיוק מקסימלי                                          │
│     • כשחשוב לא לפספס מיומנויות (high recall)                      │
│     • כשיש GPU זמין                                                │
│     • לייצור (production)                                          │
│                                                                     │
│  ⚠️ RoBERTa Base (חלופה טובה):                                     │
│     • כשצריך לחסוך זיכרון (~230MB פחות)                            │
│     • כשמספיק דיוק של 95%                                          │
│     • כאלטרנטיבה אם DeBERTa לא נטען                                │
│     • לפיתוח ובדיקות                                               │
│                                                                     │
│  ❌ MODELV2 One-Pass (לא לשימוש!):                                 │
│     • רק לניסויים ומחקר                                            │
│     • מפספס 85% מהמיומנויות                                        │
│     • אל תשתמשו בו בייצור                                          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 🚀 המלצה סופית

**השתמשו ב-DeBERTa v3 Base** 🎯

| סיבה | פירוט |
|------|-------|
| ביצועים | F1 של 97% - הגבוה מכולם |
| Recall | מזהה 95-99% מהמיומנויות |
| יציבות | אימון יציב, תוצאות עקביות |
| הבנה | Disentangled Attention מבין הקשרים מורכבים |
| Vocabulary | 128K tokens מכסה מונחים טכניים |

**228MB הנוספים שווים את הדיוק!**

---

## 🔧 פקודות שימושיות

### הרצת ניתוח עם DeBERTa
```powershell
python analyze_resume.py --text "Your resume text here" --model deberta
```

### הרצת ניתוח עם RoBERTa
```powershell
python analyze_resume.py --text "Your resume text here" --model roberta
```

### השוואת כל המודלים
```powershell
python analyze_resume.py --text "Your resume text here" --model all
```

### ניתוח קובץ עם מספר טקסטים
```powershell
python batch_analyze.py --input data.jsonl --output results.jsonl --model deberta
```

---

## 📚 מקורות ומאמרים

1. **DeBERTa Paper**: [DeBERTa: Decoding-enhanced BERT with Disentangled Attention](https://arxiv.org/abs/2006.03654) - He et al., 2020
2. **RoBERTa Paper**: [RoBERTa: A Robustly Optimized BERT Pretraining Approach](https://arxiv.org/abs/1907.11692) - Liu et al., 2019
3. **BERT Paper**: [BERT: Pre-training of Deep Bidirectional Transformers](https://arxiv.org/abs/1810.04805) - Devlin et al., 2018
4. **Attention Paper**: [Attention Is All You Need](https://arxiv.org/abs/1706.03762) - Vaswani et al., 2017
5. **HuggingFace Transformers**: [Documentation](https://huggingface.co/docs/transformers)

---

## 📝 נספח: מילון מונחים

| מונח | הסבר |
|------|------|
| **Transformer** | ארכיטקטורת רשת עצבית מבוססת Attention |
| **Attention** | מנגנון שמאפשר למודל "להתמקד" בחלקים רלוונטיים |
| **Tokenization** | פירוק טקסט לטוקנים (מילים/חלקי מילים) |
| **Embedding** | ייצוג מילה כווקטור מספרי |
| **Fine-tuning** | אימון מודל קיים על נתונים ספציפיים |
| **F1 Score** | ממוצע הרמוני של Precision ו-Recall |
| **Macro F1** | F1 ממוצע על כל הקטגוריות (משקל שווה) |
| **Pairwise** | גישה שבודקת זוגות (skill, text) |
| **One-Pass** | גישה שמנבאת הכל בהרצה אחת |
| **CLS Token** | טוקן מיוחד שמייצג את כל הקלט |
| **Hidden Size** | מימד הייצוג הפנימי (768 אצלנו) |
| **Parameters** | המשקלות הנלמדות של הרשת |

---

*מסמך נוצר • ינואר 2026 • SkillSight Project*
*גרסה 2.0 - עם הסברים מעמיקים על האלגוריתמים והיישום*
