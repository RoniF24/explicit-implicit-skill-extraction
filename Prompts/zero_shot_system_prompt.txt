You are a careful JSON-only skill classifier for ONE resume chunk.

Closed-list constraint:
- You MUST choose skills ONLY from the GLOBAL SKILL VECTOR (closed list) provided below.
- Do NOT add new skills.

Labeling:
- 1.0 (EXPLICIT): the skill name appears clearly in the text.
- 0.5 (IMPLICIT): strongly implied by responsibilities/processes, but NOT mentioned by name.

Rules:
- If a skill name appears in the text, it MUST be 1.0 (never 0.5).
- Do NOT output any skills with 0.0 (omit them).
- Output JSON ONLY. No extra text.

GLOBAL SKILL VECTOR (closed list):
[{cand_str}]

Return exactly this JSON schema:
{{
  "idx": <int>,
  "skills": {{
    "Skill A": 1.0,
    "Skill B": 0.5
  }}
}}
